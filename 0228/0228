@keyframes 애니메이션이름{	애니메이션 제작	}

animation-name: 애니메이션 이름
animation-duration: 애니메이션 실행시간
animation-delay: 엘리먼트 로드 후 애니메이션이 시작되기까지의 시간
animation-iteration-count : 애니메이션 반복횟수 지정 (infinite로 무한반복)
animation-direction : 애니메이션 종료되고 역방향/정방향으로 시작할지 결정
    1)normal: default
    2)reverse: 역방향
    3)alternate: 각 주기마다 방향 뒤집음, 첫번째는 정방향으로 시작
    4)alternate-reverse : 각 주기마다 방향 뒤집음, 첫번째는 역방향으로 시작

animation-play-state : 애니메이션 멈추거나 다시 시작
animation-timing-function : transition과 동일.
animation-fill-mode : 애니메이션 끝난후 액션을 지정 //값을 2가지 줄 수 있음
    1)none(default)
    2)forwards : 마지막 설정값 유지
    3)backwards : 첫번째 설정값 유지
    4)both : 애니메이션은 앞 뒤 양쪽 모두의 규칙을 따르므로 애니메이션 속성이 양방향으로 확장.

	

join(연결문자) : 배열 객체의 데이터를 연결문자 기준으로 1개의 문자형 데이터로 반환.
reverse : 배열 객체의 데이터 순서를 거꾸로 바꾼 후 반환
sort() : 오름차순으로 정렬
slice(index1, index2) : 원하는 인덱스 구간만큼 잘라서 배열 객체로 가져옴.
    ->begin(index1) : 잘라낼 배열의 시작
    ->end(index2) : 잘라낼 배열의 종료(end값은 잘라낼 배열에 포함되지 않음)
    ->*end가 생략될 경우, begin부터 배열의 끝까지 잘라낸다. 

splice() : 지정 데이터를 삭제하고 그 구간에 새 데이터를 삽입
concat() : 2개의 배열을 하나로 결합
pop() : 마지막 인덱스에 저장된 데이터를 삭제
push(new data) : 마지막 인덱스에 새 데이터를 삽입
shift() : 첫번째 인덱스에 저장된 데이터를 삭제
unshift(new data) : 첫번째 인덱스에 새 데이터를 삽입
length : 배열의 개수(길이)  
